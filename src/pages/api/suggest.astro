---
import {
  suggestRecipes,
  type Recipe,
  type SuggestConstraints,
} from "../../lib/recipes";
import { checkRateLimitHybrid } from "../../lib/rate-limit-hybrid";

function parseConstraints(raw: string | undefined): SuggestConstraints {
  if (!raw?.trim()) return {};
  try {
    const v = JSON.parse(raw) as unknown;
    if (!v || typeof v !== "object" || Array.isArray(v)) return {};
    return v as SuggestConstraints;
  } catch {
    return {};
  }
}

// Only POST allowed
if (Astro.request.method !== "POST") {
  return new Response("Method not allowed", { status: 405 });
}

let recipes: Recipe[] = [];
let error = "";

function parseStringArray(raw: string | undefined): string[] {
  if (!raw?.trim()) return [];
  try {
    const v = JSON.parse(raw) as unknown;
    return Array.isArray(v) ? v.map((x) => String(x)) : [];
  } catch {
    return [];
  }
}

try {
  const formData = await Astro.request.formData();
  const ingredientsRaw = formData.get("ingredients")?.toString() ?? "";
  const ingredients = ingredientsRaw
    .split(",")
    .map((i) => i.trim())
    .filter(Boolean);

  if (ingredients.length === 0) {
    error = "Please add at least one ingredient.";
  } else {
    const auth = await Astro.locals.auth();
    const uid = auth.userId ?? null;
    const rl = await checkRateLimitHybrid(
      Astro.request,
      uid,
      "suggest",
      { maxAnon: 24, maxAuth: 96, windowMs: 15 * 60 * 1000 },
      !!uid,
    );
    if (!rl.allowed) {
      return new Response(
        `<div class="error-message">Too many recipe requests. Please wait ${rl.retryAfterSeconds} seconds and try again.</div>`,
        {
          status: 429,
          headers: { "Content-Type": "text/html; charset=utf-8" },
        },
      );
    }
    const constraints = parseConstraints(
      formData.get("constraints")?.toString(),
    );
    const excludeRecipeIds = parseStringArray(
      formData.get("excludeRecipeIds")?.toString(),
    );
    recipes = await suggestRecipes(ingredients, constraints, {
      excludeRecipeIds,
    });
  }
} catch (e: unknown) {
  const msg = e instanceof Error ? e.message : "";
  const lower = msg.toLowerCase();
  if (
    /401|api key|api_key|unauthenticated|permission denied|login required/.test(
      lower,
    )
  ) {
    error =
      "Google AI could not authorize the request. If you are on Netlify, add GEMINI_API_KEY (from Google AI Studio) under Site configuration → Environment variables, save, and redeploy.";
  } else if (
    /429|quota|resource exhausted|resource_exhausted|rate limit|billing/.test(
      lower,
    )
  ) {
    error =
      "Recipe suggestions hit a rate limit or quota on your Google AI project. Wait a few minutes and try again, or check usage and billing in Google AI Studio.";
  } else {
    error = msg || "Something went wrong. Please try again.";
  }
}

function difficultyClass(d: string) {
  if (d === "easy") return "badge--easy";
  if (d === "hard") return "badge--hard";
  return "badge--medium";
}

// Encode recipe data safely as base64 to avoid attribute quote issues
function encodeRecipe(recipe: Recipe): string {
  return btoa(unescape(encodeURIComponent(JSON.stringify(recipe))));
}
---

{
  error ? (
    <div class="error-message">{error}</div>
  ) : (
    <div class="recipe-grid">
      {recipes.map((recipe) => (
        <div
          class="recipe-card"
          role="button"
          tabindex="0"
          data-recipe={encodeRecipe(recipe)}
          onclick="window.dispatchOpenRecipe(this)"
          onkeydown="if(event.key==='Enter') window.dispatchOpenRecipe(this)"
        >
          <div class="recipe-card__media">
            <img
              class="recipe-card__image"
              alt={recipe.title}
              loading="lazy"
            />
            <div class="recipe-card__placeholder recipe-card__placeholder--loading">
              Generating finished dish photo...
            </div>
          </div>
          <div class="recipe-card__header">
            <h3 class="recipe-card__title">{recipe.title}</h3>
            <button
              class="recipe-card__bookmark"
              data-recipe={encodeRecipe(recipe)}
              data-recipe-id={recipe.id}
              onclick="event.stopPropagation(); window.handleBookmark(this)"
              aria-label="Toggle bookmark"
            >
              <span>☆</span>
            </button>
          </div>
          <p class="recipe-card__desc">{recipe.description}</p>
          <div class="recipe-card__meta">
            <span class="recipe-card__time">⏱ {recipe.cookTime}</span>
            <span class={`badge ${difficultyClass(recipe.difficulty)}`}>
              {recipe.difficulty}
            </span>
          </div>
        </div>
      ))}
    </div>
  )
}

